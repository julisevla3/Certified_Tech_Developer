const {createElement} = require('../util.js');

describe('Criar um novo elemento HTML', () =>{

    test('Retornar um novo elemento', () =>{
        const text = createElement('String', 'blablabla', 'qualquer');
        expect(text).toBe();
    });
})
